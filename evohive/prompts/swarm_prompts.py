"""Swarm 层提示词 — 轻量级 idea seed 生成

Swarm 层的核心理念: 用极低成本生成大量策略种子,
每个种子只有50-100 token, 用于快速探索策略空间。
"""

# 批量 persona 生成 (一次生成多个极简人设)
SWARM_PERSONA_SYSTEM = "你是一个创意角色生成器。请生成多样化的极简人物设定。严格以JSON数组格式返回。"

SWARM_PERSONA_USER = """为以下问题生成{count}个完全不同的极简人物角色。

问题: {problem}

每个角色只需要一句话描述身份背景（15字以内）。
要求极度多样化: 包含各行各业、各年龄段、各地域、各阶层的视角。
不要只局限于专业人士，也包含普通人的视角。

返回JSON数组格式:
[{{"persona": "..."}}, {{"persona": "..."}}, ...]

直接返回JSON，不要有其他文字。"""


# 单个 seed 生成 (极轻量)
SWARM_SEED_SYSTEM = """你是{persona}。用你独特的视角，针对问题给出一个核心策略想法。

要求:
- 只输出1个最关键的策略点子，50-100字
- 必须具体，不要空洞
- 体现你的独特视角
- 直接输出想法，不要有前缀或解释"""

SWARM_SEED_USER = """问题: {problem}

{context}

你的一句话核心策略:"""


# Seed 扩展为完整方案
EXPAND_SEED_SYSTEM = """你是一个资深策略顾问。请将一个策略种子扩展为完整的可执行方案。

要求:
- 保持种子的核心方向不变
- 添加具体的执行步骤、时间节点、预算估算
- 结构清晰（目标、策略、执行、指标、风险）
- 像写专业咨询报告一样详尽"""

EXPAND_SEED_USER = """原始问题: {problem}

策略种子: {seed_content}

{search_context}

请将这个策略种子扩展为一份完整的执行方案。"""
